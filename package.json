{
  "name": "simple-chat-bot",
  "version": "1.0.0",
  "description": "bot",
  "main": "simpleChatBot.js",
  "scripts": {
    "start": "browser-sync start --server --files '**/*.css, **/*.html, **/*.js' --directory --port 3333"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.23.6"
  }
}
